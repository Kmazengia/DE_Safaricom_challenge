// This question have time complexity of n.
public class stringReveral{
//reverse string "entoto" to find "ototne" 
public static void main(String[] args)
{
String a = "entoto"
for(int i=0; i<=a.length()/2; i++)
{
char temp =a.charAt(i);
a.charAt(i) =a.charAt(a.length-i);
a.charAt(a.length-i)=temp;
}
return a;
}
